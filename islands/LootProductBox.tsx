import Component from "$store/components/ui/RateLuckBox.tsx";
import type { Props } from "$store/components/ui/RateLuckBox.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
